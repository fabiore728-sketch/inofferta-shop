# Esportazione

Il progetto è sorgente completo e può essere archiviato in ZIP.

Non includere `node_modules`, `.next` o file `.env`.

Comando opzionale:

```bash
zip -r InOfferta.shop.zip . -x "node_modules/*" ".next/*" "*.log"
```
